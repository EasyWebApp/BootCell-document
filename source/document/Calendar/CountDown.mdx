---
layout: docs
title: Count down
description:
group: Components
---

import { CountDown } from 'boot-cell';

import { Example } from '../../component/Example';

<Example>
    <CountDown endTime={new Date(new Date().getFullYear() + 1, 5, 4)} />
</Example>

```TSX
import { DOMRenderer } from 'dom-renderer';
import { CountDown } from 'boot-cell';

new DOMRenderer().render(
    <CountDown endTime={new Date(new Date().getFullYear() + 1, 5, 4)} />
);
```
