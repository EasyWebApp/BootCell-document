---
layout: docs
title: Input group
description: Easily extend form controls by adding text, buttons, or button groups on either side of textual inputs, custom selects, and custom file inputs.
group: Components
---

import { Fragment } from 'web-cell';

import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';
import { Button } from 'boot-cell/source/Form/Button';
import { DropMenu, DropMenuItem } from 'boot-cell/source/Navigator/DropMenu';

import { Example } from '../../../../source/component/Example';

## Basic example

Place one add-on or button on either side of an input. You may also place one on both sides of an input.

<Example>
    <InputGroup>
        @
        <Field placeholder="Username" aria-label="Username" />
    </InputGroup>
    <InputGroup>
        <Field
            placeholder="Recipient's username"
            aria-label="Recipient's username"
        />
        @example.com
    </InputGroup>
    <label for="basic-url">Your vanity URL</label>
    <InputGroup>
        https://example.com/users/
        <Field id="basic-url" placeholder="Username" aria-label="Username" />
    </InputGroup>
    <InputGroup>
        $
        <Field type="number" aria-label="Amount (to the nearest dollar)" />
        .00
    </InputGroup>
    <InputGroup>
        With textarea
        <Field is="textarea" aria-label="With textarea" />
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup>
            @
            <Field placeholder="Username" aria-label="Username" />
        </InputGroup>
        <InputGroup>
            <Field
                placeholder="Recipient's username"
                aria-label="Recipient's username"
            />
            @example.com
        </InputGroup>
        <label for="basic-url">Your vanity URL</label>
        <InputGroup>
            https://example.com/users/
            <Field id="basic-url" placeholder="Username" aria-label="Username" />
        </InputGroup>
        <InputGroup>
            $
            <Field type="number" aria-label="Amount (to the nearest dollar)" />
            .00
        </InputGroup>
        <InputGroup>
            With textarea
            <Field is="textarea" aria-label="With textarea" />
        </InputGroup>
    </>
);
```

## Wrapping

Input groups wrap by default via `flex-wrap: wrap` in order to accommodate custom form field validation within an input group.
You may disable this with `.flex-nowrap`.

<Example>
    <InputGroup className="flex-nowrap">
        @
        <Field placeholder="Username" aria-label="Username" />
    </InputGroup>
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <InputGroup className="flex-nowrap">
        @
        <Field placeholder="Username" aria-label="Username" />
    </InputGroup>
);
```

## Sizing

Add the relative form `size` property to the `<InputGroup />` itself
and contents within will automatically resize —
no need for repeating the form control `size` property on each element.

**Sizing on the individual input group elements isn’t supported.**

<Example>
    <InputGroup size="sm">
        Small
        <Field />
    </InputGroup>
    <InputGroup>
        Default
        <Field />
    </InputGroup>
    <InputGroup size="lg">
        Large
        <Field />
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup size="sm">
            Small
            <Field />
        </InputGroup>
        <InputGroup>
            Default
            <Field />
        </InputGroup>
        <InputGroup size="lg">
            Large
            <Field />
        </InputGroup>
    </>
);
```

## Checkboxes and radios

Place any checkbox or radio option within an input group’s addon instead of text.

<Example>
    <InputGroup>
        <input type="checkbox" aria-label="Checkbox for following text input" />
        <Field aria-label="Text input with checkbox" />
    </InputGroup>
    <InputGroup>
        <input
            type="radio"
            aria-label="Radio button for following text input"
        />
        <Field aria-label="Text input with radio button" />
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup>
            <input type="checkbox" aria-label="Checkbox for following text input" />
            <Field aria-label="Text input with checkbox" />
        </InputGroup>
        <InputGroup>
            <input
                type="radio"
                aria-label="Radio button for following text input"
            />
            <Field aria-label="Text input with radio button" />
        </InputGroup>
    </>
);
```

## Multiple inputs

While multiple `<Field />`s are supported visually,
validation styles are only available for input groups with a single `<Field />`.

<Example>
    <InputGroup>
        First and last name
        <Field aria-label="First name" />
        <Field aria-label="Last name" />
    </InputGroup>
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <InputGroup>
        First and last name
        <Field aria-label="First name" />
        <Field aria-label="Last name" />
    </InputGroup>
);
```

## Multiple addons

Multiple add-ons are supported and can be mixed with checkbox and radio input versions.

<Example>
    <InputGroup>
        <>$</>
        <>0.00</>
        <Field aria-label="Dollar amount (with dot and two decimal places)" />
    </InputGroup>
    <InputGroup>
        <Field aria-label="Dollar amount (with dot and two decimal places)" />
        <>$</>
        <>0.00</>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup>
            <>$</>
            <>0.00</>
            <Field aria-label="Dollar amount (with dot and two decimal places)" />
        </InputGroup>
        <InputGroup>
            <Field aria-label="Dollar amount (with dot and two decimal places)" />
            <>$</>
            <>0.00</>
        </InputGroup>
    </>
);
```

## Button addons

<Example>
    <InputGroup>
        <Button color="secondary" outline>
            Button
        </Button>
        <Field aria-label="Example text with button addon" />
    </InputGroup>
    <InputGroup>
        <Field
            placeholder="Recipient's username"
            aria-label="Recipient's username"
        />
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
    <InputGroup>
        <Button color="secondary" outline>
            Button
        </Button>
        <Button color="secondary" outline>
            Button
        </Button>
        <Field aria-label="Example text with two button addons" />
    </InputGroup>
    <InputGroup>
        <Field
            placeholder="Recipient's username"
            aria-label="Recipient's username with two button addons"
        />
        <Button color="secondary" outline>
            Button
        </Button>
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Button } from 'boot-cell/source/Form/Button';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
    <InputGroup>
        <Button color="secondary" outline>
            Button
        </Button>
        <Field aria-label="Example text with button addon" />
    </InputGroup>
    <InputGroup>
        <Field
            placeholder="Recipient's username"
            aria-label="Recipient's username"
        />
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
    <InputGroup>
        <Button color="secondary" outline>
            Button
        </Button>
        <Button color="secondary" outline>
            Button
        </Button>
        <Field aria-label="Example text with two button addons" />
    </InputGroup>
    <InputGroup>
        <Field
            placeholder="Recipient's username"
            aria-label="Recipient's username with two button addons"
        />
        <Button color="secondary" outline>
            Button
        </Button>
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
    </>
);
```

## Buttons with dropmenus

<Example>
    <InputGroup>
        <DropMenu buttonColor="secondary" caption="Dropdown">
            <DropMenuItem>Action</DropMenuItem>
            <DropMenuItem>Another action</DropMenuItem>
            <DropMenuItem>Something else here</DropMenuItem>
            <DropMenuItem />
            <DropMenuItem>Separated link</DropMenuItem>
        </DropMenu>
        <Field aria-label="Text input with dropdown button" />
    </InputGroup>
    <InputGroup>
        <Field aria-label="Text input with dropdown button" />
        <DropMenu buttonColor="secondary" caption="Dropdown">
            <DropMenuItem>Action</DropMenuItem>
            <DropMenuItem>Another action</DropMenuItem>
            <DropMenuItem>Something else here</DropMenuItem>
            <DropMenuItem />
            <DropMenuItem>Separated link</DropMenuItem>
        </DropMenu>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { DropMenu, DropMenuItem } from 'boot-cell/source/Navigator/DropMenu';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup>
            <DropMenu buttonColor="secondary" caption="Dropdown">
                <DropMenuItem>Action</DropMenuItem>
                <DropMenuItem>Another action</DropMenuItem>
                <DropMenuItem>Something else here</DropMenuItem>
                <DropMenuItem />
                <DropMenuItem>Separated link</DropMenuItem>
            </DropMenu>
            <Field aria-label="Text input with dropdown button" />
        </InputGroup>
        <InputGroup>
            <Field aria-label="Text input with dropdown button" />
            <DropMenu buttonColor="secondary" caption="Dropdown">
                <DropMenuItem>Action</DropMenuItem>
                <DropMenuItem>Another action</DropMenuItem>
                <DropMenuItem>Something else here</DropMenuItem>
                <DropMenuItem />
                <DropMenuItem>Separated link</DropMenuItem>
            </DropMenu>
        </InputGroup>
    </>
);
```

## Segmented buttons

<Example>
    <InputGroup>
        <DropMenu
            buttonColor="secondary"
            caption="Action"
            href="https://web-cell.dev/BootCell/"
        >
            <DropMenuItem>Action</DropMenuItem>
            <DropMenuItem>Another action</DropMenuItem>
            <DropMenuItem>Something else here</DropMenuItem>
            <DropMenuItem />
            <DropMenuItem>Separated link</DropMenuItem>
        </DropMenu>
        <Field aria-label="Text input with segmented dropdown button" />
    </InputGroup>
    <InputGroup>
        <Field aria-label="Text input with segmented dropdown button" />
        <DropMenu
            buttonColor="secondary"
            caption="Action"
            href="https://web-cell.dev/BootCell/"
        >
            <DropMenuItem>Action</DropMenuItem>
            <DropMenuItem>Another action</DropMenuItem>
            <DropMenuItem>Something else here</DropMenuItem>
            <DropMenuItem />
            <DropMenuItem>Separated link</DropMenuItem>
        </DropMenu>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { DropMenu, DropMenuItem } from 'boot-cell/source/Navigator/DropMenu';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup>
            <DropMenu
                buttonColor="secondary"
                caption="Action"
                href="https://web-cell.dev/BootCell/"
            >
                <DropMenuItem>Action</DropMenuItem>
                <DropMenuItem>Another action</DropMenuItem>
                <DropMenuItem>Something else here</DropMenuItem>
                <DropMenuItem />
                <DropMenuItem>Separated link</DropMenuItem>
            </DropMenu>
            <Field aria-label="Text input with segmented dropdown button" />
        </InputGroup>
        <InputGroup>
            <Field aria-label="Text input with segmented dropdown button" />
            <DropMenu
                buttonColor="secondary"
                caption="Action"
                href="https://web-cell.dev/BootCell/"
            >
                <DropMenuItem>Action</DropMenuItem>
                <DropMenuItem>Another action</DropMenuItem>
                <DropMenuItem>Something else here</DropMenuItem>
                <DropMenuItem />
                <DropMenuItem>Separated link</DropMenuItem>
            </DropMenu>
        </InputGroup>
    </>
);
```

## Custom forms

Input groups include support for custom selects and custom file inputs.
Browser default versions of these are not supported.

### Custom select

<Example>
    <InputGroup id="inputGroupSelect01">
        Options
        <Field is="select" id="inputGroupSelect01">
            <option selected>Choose...</option>
            {['One', 'Two', 'Three'].map((text, index) => (
                <option value={index + 1}>{text}</option>
            ))}
        </Field>
    </InputGroup>
    <InputGroup id="inputGroupSelect02">
        <Field is="select" id="inputGroupSelect02">
            <option selected>Choose...</option>
            {['One', 'Two', 'Three'].map((text, index) => (
                <option value={index + 1}>{text}</option>
            ))}
        </Field>
        Options
    </InputGroup>
    <InputGroup id="inputGroupSelect03">
        <Button color="secondary" outline>
            Button
        </Button>
        <Field
            is="select"
            id="inputGroupSelect03"
            aria-label="Example select with button addon"
        >
            <option selected>Choose...</option>
            {['One', 'Two', 'Three'].map((text, index) => (
                <option value={index + 1}>{text}</option>
            ))}
        </Field>
    </InputGroup>
    <InputGroup id="inputGroupSelect04">
        <Field
            is="select"
            id="inputGroupSelect04"
            aria-label="Example select with button addon"
        >
            <option selected>Choose...</option>
            {['One', 'Two', 'Three'].map((text, index) => (
                <option value={index + 1}>{text}</option>
            ))}
        </Field>
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';

render(
    <>
        <InputGroup id="inputGroupSelect01">
            Options
            <Field is="select" id="inputGroupSelect01">
                <option selected>Choose...</option>
                {['One', 'Two', 'Three'].map((text, index) => (
                    <option value={index + 1}>{text}</option>
                ))}
            </Field>
        </InputGroup>
        <InputGroup id="inputGroupSelect02">
            <Field is="select" id="inputGroupSelect02">
                <option selected>Choose...</option>
                {['One', 'Two', 'Three'].map((text, index) => (
                    <option value={index + 1}>{text}</option>
                ))}
            </Field>
            Options
        </InputGroup>
        <InputGroup id="inputGroupSelect03">
            <Button color="secondary" outline>
                Button
            </Button>
            <Field
                is="select"
                id="inputGroupSelect03"
                aria-label="Example select with button addon"
            >
                <option selected>Choose...</option>
                {['One', 'Two', 'Three'].map((text, index) => (
                    <option value={index + 1}>{text}</option>
                ))}
            </Field>
        </InputGroup>
        <InputGroup id="inputGroupSelect04">
            <Field
                is="select"
                id="inputGroupSelect04"
                aria-label="Example select with button addon"
            >
                <option selected>Choose...</option>
                {['One', 'Two', 'Three'].map((text, index) => (
                    <option value={index + 1}>{text}</option>
                ))}
            </Field>
            <Button color="secondary" outline>
                Button
            </Button>
        </InputGroup>
    </>
);
```

### Custom file input

<Example>
    <InputGroup>
        Upload
        <Field type="file" />
    </InputGroup>
    <InputGroup>
        <Field type="file" />
        Upload
    </InputGroup>
    <InputGroup>
        <Button color="secondary" outline>
            Button
        </Button>
        <Field type="file" />
    </InputGroup>
    <InputGroup>
        <Field type="file" />
        <Button color="secondary" outline>
            Button
        </Button>
    </InputGroup>
</Example>

```TSX
import { render, createCell, Fragment } from 'web-cell';
import { InputGroup } from 'boot-cell/source/Form/InputGroup';
import { Field } from 'boot-cell/source/Form/Field';
import { Button } from 'boot-cell/source/Form/Button';

render(
    <>
        <InputGroup>
            Upload
            <Field type="file" />
        </InputGroup>
        <InputGroup>
            <Field type="file" />
            Upload
        </InputGroup>
        <InputGroup>
            <Button color="secondary" outline>
                Button
            </Button>
            <Field type="file" />
        </InputGroup>
        <InputGroup>
            <Field type="file" />
            <Button color="secondary" outline>
                Button
            </Button>
        </InputGroup>
    </>
);
```

## Accessibility

Screen readers will have trouble with your forms if you don’t include a label for every input.
For these input groups, ensure that any additional label or functionality is conveyed to assistive technologies.

The exact technique to be used (`<label>` elements hidden using the `.sr-only` class,
or use of the `aria-label` and `aria-labelledby` attributes, possibly in combination with `aria-describedby`)
and what additional information will need to be conveyed will vary depending on the exact type of interface widget you’re implementing.
The examples in this section provide a few suggested, case-specific approaches.
